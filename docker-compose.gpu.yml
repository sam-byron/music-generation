services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile.bak.gpu
    tty: true
    volumes:
      - ./app:/app
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    env_file:
     - ./.env
    